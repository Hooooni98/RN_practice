import React, {Fragment, Component} from 'react';
import {
  SafeAreaView,
  ScrollView,
  StatusBar,
  StyleSheet,
  Text,
  useColorScheme,
  View,
} from 'react-native';

class App extends Component {
  render() {
    return (
      <SafeAreaView>
        <Text>"TEST"</Text>
      </SafeAreaView>
    );
  }
}

const styles = StyleSheet.create({});

export default App;
